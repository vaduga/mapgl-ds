{
  "name": "vaduga-mapgl-datasource",
  "version": "0.1.0",
  "type": "module",
  "packageManager": "npm@11.18.0",
  "author": "Vadim Pyatakov",
  "license": "GPL-3.0",
  "scripts": {
    "setup": "rustup target add wasm32-unknown-unknown && cargo install wasm-pack cargo-watch && npm install && npm run build:wasm",
    "dev": "echo \"$(date +%H:%M:%S) [WASM] Initial build...\" && npm run build:wasm && echo \"$(date +%H:%M:%S) [WASM] Initial build done\" && echo \"$(date +%H:%M:%S) [DEV] Starting watchers...\" && rspack build --config rspack.config.js --watch & (cd wasm-core && cargo watch --postpone -w src -s 'echo \"$(date +%H:%M:%S) [WASM] Compiling...\" && wasm-pack build --target web && echo \"$(date +%H:%M:%S) [WASM] Build done\"') & wait",
    "dev:ts": "echo \"$(date +%H:%M:%S) [DEV:TS] Starting watcher...\" && rspack build --config rspack.config.js --watch",
    "dev:rust": "cd wasm-core && cargo watch -w src -s 'echo \"$(date +%H:%M:%S) [WASM] Compiling...\" && wasm-pack build --target web && echo \"$(date +%H:%M:%S) [WASM] Build done\"'",
    "build": "npm run build:wasm && npm run build:rspack",
    "build:rspack": "echo \"$(date +%H:%M:%S) [TS] Building Tempo datasource...\" && node scripts/clean-plugin-dist.mjs && rspack build --config rspack.config.js --env production",
    "build:webpack": "echo \"$(date +%H:%M:%S) [TS] Building Tempo datasource with Webpack...\" && node scripts/clean-plugin-dist.mjs && webpack --config webpack.config.js --env production",
    "build:wasm": "echo \"$(date +%H:%M:%S) [WASM] Building...\" && cd wasm-core && wasm-pack build --target web && echo \"$(date +%H:%M:%S) [WASM] Build done\"",
    "test": "npm run test:ts && npm run test:rust",
    "test:ts": "jest --runInBand",
    "test:rust": "cargo test -p agent-core",
    "test:watch": "jest --watch",
    "lint": "npm run lint:ts && npm run lint:rust",
    "lint:ts": "biome check src/ tests/unit/tempo-dataframe-datasource.test.ts scripts/",
    "lint:rust": "cargo clippy -p agent-core -- -D warnings && cargo fmt -p agent-core -- --check",
    "lint:fix": "npm run lint:fix:ts && npm run lint:fix:rust",
    "lint:fix:ts": "biome check --write --unsafe src/ tests/unit/tempo-dataframe-datasource.test.ts scripts/",
    "lint:fix:rust": "cargo fmt -p agent-core && cargo clippy -p agent-core --fix --allow-dirty -- -D warnings",
    "format": "npm run format:ts && npm run format:rust",
    "format:ts": "biome format --write src/ tests/unit/tempo-dataframe-datasource.test.ts scripts/",
    "format:rust": "cargo fmt -p agent-core",
    "format:check": "npm run format:check:ts && npm run format:check:rust",
    "format:check:ts": "biome format src/ tests/unit/tempo-dataframe-datasource.test.ts scripts/",
    "format:check:rust": "cargo fmt -p agent-core -- --check",
    "package": "echo \"$(date +%H:%M:%S) [PKG] Packaging...\" && npm run build && VER=$(grep -m1 '\"version\"' package.json | cut -d'\"' -f4) && rm -f dist/vaduga-mapgl-datasource*.tar.gz && cd dist && tar czf vaduga-mapgl-datasource-$VER.tar.gz vaduga-mapgl-datasource/ && echo \"$(date +%H:%M:%S) [PKG] Done: vaduga-mapgl-datasource-$VER.tar.gz\"",
    "dev:reload": "echo \"$(date +%H:%M:%S) [RELOAD] Building and restarting Grafana...\" && npm run build && docker compose up -d grafana --force-recreate && echo \"$(date +%H:%M:%S) [RELOAD] Done\"",
    "verify": "echo \"$(date +%H:%M:%S) [VERIFY] Starting...\" && npm run lint && npm run test && npm run build:rspack && echo \"$(date +%H:%M:%S) [VERIFY] All checks passed\""
  },
  "dependencies": {
    "@grafana/data": "^12.0.0",
    "@grafana/runtime": "^12.0.0",
    "@grafana/ui": "^12.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.9",
    "@fission-ai/openspec": "^1.4.1",
    "@happy-dom/global-registrator": "^20.8.9",
    "@rspack/cli": "^1.7.10",
    "@rspack/core": "^1.7.10",
    "@swc/core": "^1.15.47",
    "@swc/jest": "^0.2.39",
    "@types/jest": "^30.0.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "copy-webpack-plugin": "^14.0.0",
    "jest": "^30.2.0",
    "jest-environment-jsdom": "^30.2.0",
    "swc-loader": "^0.2.7",
    "terser-webpack-plugin": "^5.3.16",
    "typescript": "^5.3.0",
    "webpack": "^5.109.2",
    "webpack-cli": "^7.2.2"
  }
}
